{
  "name": "cpintel-desktop",
  "version": "0.1.0",
  "description": "CPIntel Desktop App",
  "main": "dist/main.js",
  "scripts": {
    "dev": "concurrently \"tsc -w\" \"cross-env NODE_ENV=development electron .\"",
    "build": "tsc && node scripts/stamp-server.mjs && electron-builder",
    "pack": "tsc && node scripts/stamp-server.mjs && electron-builder --dir",
    "test": "tsc && node --test dist"
  },
  "dependencies": {
    "electron-store": "^8.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "concurrently": "^8.2.2",
    "cross-env": "^10.1.0",
    "electron": "^41.7.1",
    "electron-builder": "^26.15.3",
    "typescript": "^5.4.5"
  },
  "build": {
    "appId": "com.cpintel.desktop",
    "productName": "CPIntel",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "!dist/**/*.test.js"
    ],
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Development"
    },
    "mac": {
      "target": [
        "dmg"
      ],
      "category": "public.app-category.developer-tools"
    },
    "win": {
      "target": [
        "nsis"
      ]
    }
  }
}
